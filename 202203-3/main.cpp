#include <bits/stdc++.h>
using namespace std;

int l[1000]; // 计算节点到可用区的对应

int main()
{
    int n, m; // 计算节点数目、可用区的数目
    int g;//计算任务的组数

    scanf("%d %d", &n, &m);
    for (int i = 0; i < n; i++)
        scanf("%d", &(l[i]));
}